# Optional: override config path
# PAPER_CONFIG=./config.toml

# Financial Context auth is handled by the Financial Context CLI: `fcontext auth login`
# Optional overrides if your fcontext install reads env vars:
# FCONTEXT_API_KEY=
# FCONTEXT_ACCESS_TOKEN=

# Logging: info | debug | trace
RUST_LOG=info